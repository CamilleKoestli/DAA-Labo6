/**
 * Authors : Koestli Camille / Oliveira Vitoria
 * Description : This enum class represents the possible types of phone numbers associated with a contact.
 *               The types include HOME, OFFICE, MOBILE, and FAX, and are used for categorizing phone numbers.
 */

package ch.heigvd.iict.and.rest.models

enum class PhoneType {
    HOME, OFFICE, MOBILE, FAX
}